{
  "name": "@zerosnacks/lifeforms",
  "license": "AGPL-3.0-only",
  "private": true,
  "version": "0.1.0",
  "description": "Carbon bearing NFT, allows you to store BCT (Base Carbon Tonne) carbon credits inside of it.",
  "files": [
    "src/**/*.sol"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zerosnacks/lifeforms.git"
  },
  "devDependencies": {
    "prettier": "^2.3.1",
    "prettier-plugin-solidity": "^1.0.0-beta.13",
    "solhint": "^3.3.6",
    "solhint-plugin-prettier": "^0.0.5"
  },
  "scripts": {
    "lint": "prettier --check src/**/*.sol && solhint --config .solhint.json src/**/*.sol",
    "lint:fix": "prettier --write src/**/*.sol && solhint --config .solhint.json src/**/*.sol --fix"
  }
}
